apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: markets
  namespace: lux-gateway
  annotations:
    kubernetes.io/ingress.class: "hanzo"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - markets.lux.network
      secretName: markets-tls
  rules:
    - host: markets.lux.network
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: markets
                port:
                  number: 80
